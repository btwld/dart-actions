name: CI Workflow

on:
  workflow_call:
    inputs:
      flutter-version:
        description: 'Flutter version'
        required: false
        default: "stable"
        type: string
      run-dcm:
        description: 'Should run DCM'
        required: false
        default: true
        type: boolean
      melos-commands:
        description: 'List of melos commands to run (one per line)'
        required: false
        default: ""
        type: string
    secrets:
      token:
        description: 'Github token'
        required: true

jobs:
  test:
    name: Run tests for all packages
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout mix repo
        uses: actions/checkout@v4
      
      - name: Install FVM
        shell: bash
        run: |
          curl -fsSL https://fvm.app/install.sh | bash
          fvm use ${{ inputs.flutter-version }} --force

      - uses: kuhnroyal/flutter-fvm-config-action@v2
        id: fvm-config-action

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ steps.fvm-config-action.outputs.FLUTTER_VERSION }}
          channel: ${{ steps.fvm-config-action.outputs.FLUTTER_CHANNEL }}

      - name: Flutter version
        shell: bash
        run: flutter --version

      - name: Setup Melos
        uses: bluefireteam/melos-action@v3

      - name: Get dependencies
        shell: bash
        run: melos bootstrap

      - uses: invertase/github-action-dart-analyzer@v1
        with:
          fatal-infos: false

      - name: Install DCM
        if: ${{ inputs.run-dcm }}
        uses: CQLabs/setup-dcm@v2
        with:
          github-token: ${{ secrets.token }}

      - name: Run melos commands
        if: ${{ inputs.melos-commands != '' }}
        shell: bash    
        run: ${{ inputs.melos-commands }}

      - name: Run Test
        run: |
          melos run ci
        shell: bash